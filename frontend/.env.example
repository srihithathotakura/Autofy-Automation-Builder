# Leave this unset for local development — CRA's "proxy" field in package.json
# forwards /api requests to http://localhost:5000 automatically.
#
# For production (Vercel/Netlify), set this to your deployed backend URL,
# e.g.:
# REACT_APP_API_BASE=https://autofy-backend.onrender.com
REACT_APP_API_BASE=
